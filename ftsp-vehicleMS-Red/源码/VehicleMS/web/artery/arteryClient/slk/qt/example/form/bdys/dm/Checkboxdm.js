// 表单元素-代码-Checkbox代码

/**
 * 客户端(button_check1)
 * 
 * @param  rc 系统提供的AJAX调用对象
 */
function button_check1_onClickClient (rc){
	Artery.showMessage("Check1的值为："+Artery.get("check1").getValue());
}

/**
 * 客户端(button_check2)
 * 
 * @param  rc 系统提供的AJAX调用对象
 */
function button_check2_onClickClient (rc){
	Artery.showMessage("Check2的值为："+Artery.get("check2").getValue());
}

/**
 * 客户端(button_check3)
 * 
 * @param  rc 系统提供的AJAX调用对象
 */
function button_check3_onClickClient (rc){
	Artery.showMessage("Check3的值为："+Artery.get("check3").getValue());
}

/**
 * 客户端(button_check4)
 * 
 * @param  rc 系统提供的AJAX调用对象
 */
function button_check4_onClickClient (rc){
	Artery.showMessage("Check4的值为："+Artery.get("check4").getValue());
}