// **********************************************//
// _faBlankLayout_method 客户端脚本
// 
// @author Artery
// @date 2011-10-21
// @id 91ba96805de645c681e0aa4f94b328b7
// **********************************************//

/**
 * onClickClient(button04294)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button04294_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').alignTo(Artery.get("blankPanel3ad0c").el,"tl",[50,50]);
}

/**
 * onClickClient(buttonfccca)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function buttonfccca_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').center();
}

/**
 * onClickClient(button04a36)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button04a36_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').hide();
}

/**
 * onClickClient(button25d25)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button25d25_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').show();
}

/**
 * onClickClient(button19b70)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button19b70_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').readAllSub();
}

/**
 * onClickClient(button49b52)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button49b52_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').editAllSub();
}

/**
 * onClickClient(button11304)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button11304_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').disableAllSub();
}

/**
 * onClickClient(button64b52)
 * 
 * @param rc
 *            系统提供的AJAX调用对象
 */
function button64b52_onClickClient(rc) {
	Artery.get('faBlankLayout9d239').enableAllSub();
}
